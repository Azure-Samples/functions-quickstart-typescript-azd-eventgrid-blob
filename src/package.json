{
  "name": "functions-quickstart-typescript-azd-eventgrid-blob",
  "version": "1.0.0",
  "description": "Azure Functions TypeScript Event Grid Blob Trigger sample using Azure Developer CLI",
  "main": "dist/functions/*.js",
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.0.0",
    "@azure/identity": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}