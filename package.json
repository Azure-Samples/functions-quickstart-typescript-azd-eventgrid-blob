{
  "name": "functions-quickstart-typescript-azd-eventgrid-blob",
  "version": "1.0.0",
  "description": "Azure Functions TypeScript Event Grid Blob Trigger using Azure Developer CLI",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.24.0"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "typescript": "^4.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}